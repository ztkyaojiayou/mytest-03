<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:context="http://www.springframework.org/schema/context"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<context:property-placeholder
			location="classpath:conf/custom/env/redis.properties"
			ignore-unresolvable="true" />

	<bean id="jedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
		<property name="maxIdle" value="${maxIdle}" />
		<property name="minIdle" value="${minIdle}" />
		<property name="maxTotal" value="${maxTotal}" />
	</bean>

	<bean id="redisConfig" class="com.rezen.framework.redis.SharedJRedisConfig">
		<property name="jedisPoolConfig" ref="jedisPoolConfig" />
		<property name="ipPorts" value="${ipPorts}"/>
		<property name="timeout" value="${timeout}"/>
		<property name="password" value="${password}"/>
	</bean>

	<bean id="sharedJedisClient" class="com.rezen.framework.redis.client.SharedJedisClient">
		<property name="redisConfig" ref="redisConfig" />
	</bean>
</beans>
